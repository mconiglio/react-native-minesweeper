import React from 'react';
import { SafeAreaView } from 'react-native';
import Game from './components/Game';

const App = () => {
  return (
    <SafeAreaView>
      <Game />
    </SafeAreaView>
  );
};

export default App;
